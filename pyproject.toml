[project]
name = "01-transformer-classificator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
commit = [
    "pre-commit>=4.2.0",
]
dev = [
    "ruff>=0.12.0",
]
