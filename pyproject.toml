[project]
name = "01-transformer-classificator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pandas>=2.3.0",
    "pyarrow>=20.0.0",
    "sentence-transformers>=4.1.0",
    "torch>=2.7.1",
    "torchvision>=0.22.1",
]

[dependency-groups]
commit = [
    "pre-commit>=4.2.0",
]
dev = [
    "ipykernel>=6.29.5",
    "ruff>=0.12.0",
]
